# Comma-separated burner private keys (0x...). Rotated after each win (streak dodge).
# NEVER commit real keys. Fund each with Unichain ETH for mint + gas.
PRIVATE_KEYS=0xYOUR_KEY_1,0xYOUR_KEY_2

# Optional
# UNICRED_RPC=https://mainnet.unichain.org
