# Push to private GitHub (amir64564/unicred-gpu-miner-private)

Repo already exists. From this tree:

```bash
git push origin master
```

`gh` should be logged in as **amir64564**. Never commit `.env` (gitignored).
